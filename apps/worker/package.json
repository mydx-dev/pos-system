{
    "name": "@mydx-pos/worker",
    "private": true,
    "type": "module",
    "dependencies": {
        "@better-auth/drizzle-adapter": "^1.6.23",
        "@mydx-pos/shared": "workspace:*",
        "better-auth": "^1.6.23",
        "drizzle-orm": "^0.45.2"
    },
    "devDependencies": {
        "typescript": "^6.0.3",
        "wrangler": "^4.49.0"
    },
    "scripts": {
        "dev": "wrangler dev",
        "build": "wrangler deploy --dry-run",
        "test": "vitest --run",
        "typecheck": "tsc --noEmit",
        "check": "pnpm typecheck && pnpm build",
        "db:migrations:list": "wrangler d1 migrations list mydx-pos-local",
        "db:migrations:apply:local": "wrangler d1 migrations apply mydx-pos-local --local",
        "db:migrations:apply:remote": "wrangler d1 migrations apply mydx-pos-local --remote",
        "types": "wrangler types"
    }
}
